version: 2

models:
  - name: fct_page_views
    description: "daily product web page views"
    columns:
      - name: key
        description: "The primary key for this table"
        tests:
          - unique
  - name: fct_user_sessions
    description: "sessions info joined with dim_users"  
  - name: fct_prod_sessions_orders
    description: "aggregated summary of product orders and events"
      
          